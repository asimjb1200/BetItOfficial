"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var express = require('express');
var path = require('path');
var cookieParser = require('cookie-parser');
var bodyParser = require('body-parser');
var logger = require('morgan');
var fs = require('fs');
var indexRouter = require('./routes/index');
var usersRouter = require('./routes/users');
var sportsHandler = require('./routes/sports');
var walletHandler = require('./routes/wallet_routes');
var token_auth_1 = require("./tokens/token_auth");
var accessLogStream = fs.createWriteStream(path.join(__dirname, '/logs/access.log'), { flags: 'a' });
var app = express();
app.use(logger('dev'));
app.use(logger('combined', { stream: accessLogStream }));
app.use(express.json());
app.use(express.urlencoded({ extended: false }));
app.use(cookieParser());
app.use(bodyParser.json());
app.use(express.static(path.join(__dirname, 'public')));
app.use('/', indexRouter);
app.use('/btc-handler', token_auth_1.authenticateJWT, walletHandler);
app.use('/sports-handler', token_auth_1.authenticateJWT, sportsHandler);
app.use('/users', usersRouter);
module.exports = app;
